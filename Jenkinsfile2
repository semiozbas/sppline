pipeline {
  agent {
    kubernetes {
      	cloud 'anthos-gke'
      }
    }
	stages {
        stage('build') {
            steps {
                sh '
		sleep 400
		pip install flask'
            }
        }
        stage('test') {
            steps {
                sh 'python test.py'
            }
            post {
                always {junit 'test-reports/*.xml'}
            }
        }
    }

}
